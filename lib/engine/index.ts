export type * from "./types";

// The engine itself lands at M2, corpus first (TEC-050 step 2).
// This module exists now so the import boundary of TEC-007 is enforced by
// lint from the first commit rather than retrofitted later.
