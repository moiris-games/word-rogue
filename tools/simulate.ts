import { summary } from "./_report";

// TEC-051 / TST-030: plays whole runs headlessly with the greedy and novice
// bot policies to tune the Silence ladder of ENC-016 against TST-031.
// Lands at M4, once the encounter loop exists.

summary("simulate", { runs: 0, policies: 0, note: "pending-M4" });
